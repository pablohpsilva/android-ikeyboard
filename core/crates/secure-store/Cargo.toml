[package]
name = "featherkey-secure-store"
version = "0.0.0"
publish = false
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Encrypted persistence of personal data; implements the SecureStore port."

[package.metadata.featherkey]
layer = "adapter"

[dependencies]
featherkey-contracts = { path = "../contracts" }
redb = "2.6.3"
aes-gcm = "0.11.0"
zeroize = "1.9.0"

[lints]
workspace = true

[dev-dependencies]
tempfile = "3.27.0"
