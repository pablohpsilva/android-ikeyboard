[package]
name = "featherkey-neural-ranker"
version = "0.0.0"
publish = false
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Tiny neural re-ranker: a 9-slot feature vector (including the confidence-gated lm_logprob LM term) and a cold-start prior that reproduces the linear candidate ranking."

[package.metadata.featherkey]
layer = "domain"

[lints]
workspace = true

[dependencies]
featherkey-nn = { path = "../nn" }
featherkey-contracts = { path = "../contracts" }

[dev-dependencies]
featherkey-candidate-ranker = { path = "../candidate-ranker" }
featherkey-language-momentum = { path = "../language-momentum" }
proptest = "1.11.0"
