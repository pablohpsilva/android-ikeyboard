[package]
name = "featherkey-editing"
version = "0.0.0"
publish = false
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Cursor movement and text-selection operations."

[package.metadata.featherkey]
layer = "domain"

[dependencies]
featherkey-kernel = { path = "../kernel" }


[lints]
workspace = true
