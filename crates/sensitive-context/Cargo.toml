[package]
name = "featherkey-sensitive-context"
version = "0.0.0"
publish = false
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Detect password/sensitive fields; suppress learning and prediction."

[package.metadata.featherkey]
layer = "domain"

[dependencies]
featherkey-contracts = { path = "../contracts" }

[lints]
workspace = true
