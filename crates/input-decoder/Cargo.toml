[package]
name = "featherkey-input-decoder"
version = "0.0.0"
publish = false
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Map touch coordinates + key geometry to the intended key and a ranked candidate set. The accuracy engine."

[package.metadata.featherkey]
layer = "domain"

[dependencies]
featherkey-kernel = { path = "../kernel" }
featherkey-layout-engine = { path = "../layout-engine" }
featherkey-touch-model = { path = "../touch-model" }

[lints]
workspace = true
