[package]
name = "featherkey-dictionary"
version = "0.0.0"
publish = false
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Compact per-language lexicons; prefix/fuzzy lookup."

[package.metadata.featherkey]
layer = "domain"

[dependencies]
fst = "0.4.7"


[lints]
workspace = true
