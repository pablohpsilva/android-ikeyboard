[package]
name = "featherkey-prediction"
version = "0.0.0"
publish = false
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Autocomplete via prefix completion over the active lexicons (Predictor port). Frequency/n-gram ranking is v1.x, once frequency data is available."

[package.metadata.featherkey]
layer = "domain"

[dependencies]
featherkey-contracts = { path = "../contracts" }
featherkey-dictionary = { path = "../dictionary" }

[lints]
workspace = true
