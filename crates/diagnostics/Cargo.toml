[package]
name = "featherkey-diagnostics"
version = "0.0.0"
publish = false
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Opt-in, content-free local diagnostics ring buffer."

[package.metadata.featherkey]
layer = "domain"

[dependencies]
featherkey-contracts = { path = "../contracts" }

[lints]
workspace = true
